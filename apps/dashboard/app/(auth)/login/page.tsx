// =============================================================================
// Page: /login
//
// What goes here:
//  - Login form with email + password fields
//  - Form validation (client-side with react-hook-form + zod, or native HTML5)
//  - Submit handler: POST /v1/auth/login → store JWT in httpOnly cookie
//  - Error display for invalid credentials
//  - Link to /register
//  - Redirect to /overview on success
//  - Use shadcn/ui Card, Input, Button, Label components
// =============================================================================

export default function LoginPage(): JSX.Element {
  return (
    <main>
      {/* TODO: Implement login form */}
      <h1>Login</h1>
    </main>
  );
}
