// =============================================================================
// Page: /overview (Dashboard Home)
//
// What goes here:
//  - Summary stat cards: Total Requests, Allowed %, Denied %, Active Rules
//  - Requests over time chart (Recharts LineChart or AreaChart)
//  - Top 5 rules by request volume (Recharts BarChart)
//  - Recent request logs table (last 10)
//  - Data fetched server-side via fetch() from the API (Next.js RSC)
//  - Refresh every 30 seconds (use React 18 startTransition + revalidate)
// =============================================================================

export default function OverviewPage(): JSX.Element {
  return (
    <main>
      {/* TODO: Implement overview dashboard */}
      <h1>Overview</h1>
    </main>
  );
}
