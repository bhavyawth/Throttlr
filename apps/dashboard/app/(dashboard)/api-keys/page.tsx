// =============================================================================
// Page: /api-keys (API Key Management)
//
// What goes here:
//  - Table of API keys (name, masked key, isActive, createdAt)
//  - Create API Key button → shows the generated raw key ONCE in a modal
//  - Toggle active/inactive per key
//  - Delete key (with confirmation dialog)
//  - Copy-to-clipboard for the full key on creation
//  - Use shadcn/ui Table, Dialog, Badge, Button components
// =============================================================================

export default function ApiKeysPage(): JSX.Element {
  return (
    <main>
      {/* TODO: Implement API keys management page */}
      <h1>API Keys</h1>
    </main>
  );
}
