// =============================================================================
// Page: /rules (Rule Management)
//
// What goes here:
//  - Table of all rate limit rules (name, algorithm, window, maxRequests, createdAt)
//  - Create Rule button → opens a modal/drawer with RuleForm
//  - Edit / Delete actions per row
//  - RuleForm fields: name, algorithm (select), windowSizeSeconds, maxRequests, apiKeyId
//  - Optimistic UI updates with SWR or React Query
//  - Empty state when no rules exist
// =============================================================================

export default function RulesPage(): JSX.Element {
  return (
    <main>
      {/* TODO: Implement rules management page */}
      <h1>Rules</h1>
    </main>
  );
}
